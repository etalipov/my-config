# Config my linux workstation:

* `sudo apt update && sudo apt upgrade -y`
* `sudo apt install -y git curl wget tmux zsh tree neovim make postgresql`
* install [oh my zsh](https://github.com/ohmyzsh/ohmyzsh/wiki)
* install [oh my tmux](https://github.com/gpakosz/.tmux)
* copy `nvim/` in `~/.config`
* copy `tmux/` in `~/.config`
